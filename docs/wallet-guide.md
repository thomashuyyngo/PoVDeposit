# Wallet guide

Freighter and Rabet appear in the connection dialog. Unlock the extension, approve public-address access and select Mainnet. The application rechecks the network before a core transaction and does not treat a restored public address as authorization.

Freighter uses the official browser API. Every core operation displays its Mainnet transaction for wallet approval before submission.
