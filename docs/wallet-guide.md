# Wallet guide

Freighter and Rabet appear in the connection dialog. Unlock the extension, approve public-address access and select Testnet. The application rechecks the network before a core transaction and does not treat a restored public address as authorization.

Freighter uses the official browser API. Rabet availability depends on its injected signing interface; a full current-Testnet smoke flow must be recorded before declaring Rabet complete.
