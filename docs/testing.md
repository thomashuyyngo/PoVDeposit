# Testing

CI covers unit/integration tests, strict TypeScript, production builds, Playwright desktop/mobile flows, dependency audit, secret scanning, Rust formatting, clippy, contract tests and the Wasm target build.

Automated checks do not replace real Testnet evidence. Each accepted smoke flow must name the deployed commit, contract, public wallet role, booking ID, transaction hashes and result.
