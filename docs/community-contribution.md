# Community contribution

The reusable attendance-deposit pattern documents shared booking states, transition validation, Rust-aligned types, event parsing and error mapping. The QR format is booking-bound, nonce-based, short-lived, signed/hashed and one-time.

The implementation and integration guidance are maintained in this public repository for Stellar builders.
