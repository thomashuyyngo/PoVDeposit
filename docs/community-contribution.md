# Community contribution

The planned reusable package is `@project/soroban-escrow-state-machine`: shared booking states, transition validation, Rust-aligned types, event parsing and error mapping. The QR format is booking-bound, nonce-based, short-lived, signed/hashed and one-time.

The package is not yet published. No package registry or external contribution URL is invented.
