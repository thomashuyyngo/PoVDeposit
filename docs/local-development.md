# Local development

Follow README setup, use the ignored API `.env`, run Prisma generate/migrate/seed, then build before starting so Nest can serve `apps/web/out`. Test data is labeled Testnet sample data and is never user-growth evidence.
