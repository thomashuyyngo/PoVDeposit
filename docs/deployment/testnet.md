# Testnet deployment

Railway builds the Next.js export and NestJS API, applies Prisma migrations to Neon and serves the web application from one service. Keep database, session, QR, evidence and deployment credentials in protected variables.

Deploy and initialize the optimized current Wasm once. Record contract ID, accepted asset, deployment/initialization transactions, deployer public address, commit SHA, date, application URL and health URL. Then complete fresh Freighter and Rabet lifecycle tests.
