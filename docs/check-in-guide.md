# Check-in guide

The server issues a random one-time challenge bound to booking, renter, nonce and a two-minute expiration. The QR contains no private property or identity data. A consumed, altered, mismatched or expired token is rejected. Camera access is optional: staff can provide the same short-lived manual code.
