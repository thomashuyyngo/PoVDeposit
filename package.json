{
  "name": "pov-deposit",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.18.3",
  "scripts": {
    "postinstall": "corepack pnpm --filter @pov-deposit/api exec prisma generate",
    "build": "corepack pnpm -r build",
    "lint": "corepack pnpm -r lint",
    "typecheck": "corepack pnpm -r typecheck",
    "test": "corepack pnpm -r test",
    "test:contracts": "cargo test --manifest-path contracts/Cargo.toml",
    "start": "corepack pnpm --filter @pov-deposit/api start",
    "migrate": "corepack pnpm --filter @pov-deposit/api migrate",
    "seed": "corepack pnpm --filter @pov-deposit/api seed"
  },
  "pnpm": {
    "overrides": {
      "effect": "3.20.0",
      "js-yaml": "5.2.2",
      "postcss": "8.5.18",
      "sharp": "0.35.0"
    }
  }
}
